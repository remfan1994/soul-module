from soul_module import SoulModule

# Initialize module
engine = SoulModule(dim=5, dt=0.1, time_steps=50)

# Run the reasoning engine
engine.run()

# Access current state
C, W, M, B, R, P = engine.state()
print("Final State:")
print(f"C={C}, W={W}, M={M}, B={B}, R={R}, P={P}")
